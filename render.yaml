services:
  - type: web
    name: telegram-billbot
    env: python
    runtime: python-3.11
    buildCommand: |
      apt-get update
      apt-get install -y tesseract-ocr
      pip install -r requirements.txt
    startCommand: python main.py
